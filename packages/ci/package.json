{
  "name": "@signoff/ci",
  "version": "0.1.2",
  "description": "SignOff CI check — fails a PR unless its feature's plan is approved in the vault",
  "type": "module",
  "main": "dist/cli.js",
  "bin": { "signoff-ci": "dist/cli.js" },
  "files": ["dist"],
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit",
    "prepublishOnly": "npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": { "@signoff/vault-core": "*" },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vitest": "^3.2.4"
  },
  "engines": { "node": ">=20.0.0" }
}
